{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--executable-path", "/opt/pw-browsers/chromium-1194/chrome-linux/chrome", "--headless", "--isolated"]
    }
  }
}
